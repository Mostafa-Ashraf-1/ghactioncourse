name: First workflow

on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print a welocme message
        run: echo "Welocome to our first GihHub Actions Workflow!"
